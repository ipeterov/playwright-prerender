"""A tiny fake SPA: static HTML pages whose inline scripts exercise every
behaviour in the contract. Used by the flow tests and by
docker-compose.example.yml. Run standalone with
`uv run python -m tests.stub_app --port 8080`."""
